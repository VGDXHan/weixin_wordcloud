{
  "name": "weixin-wordcloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "选择微信会话，一键生成聊天词云（Tauri 2.x，复用 WeFlow 原生库读取微信 4.x 数据）",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "wordcloud": "^1.2.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "@types/wordcloud": "^1.2.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
